# Ad Management System — Python dependencies
# Install: pip install -r requirements.txt

# Platform SDKs
python-amazon-ad-api>=2.0.0       # Amazon Advertising API
facebook-business>=18.0.0         # Meta Marketing API

# HTTP & utilities
requests>=2.31.0
python-dotenv>=1.0.0

# KDP automation (optional — only needed if PLAYWRIGHT_KDP_ENABLED=true)
# playwright>=1.40.0              # uncomment and pip install playwright && playwright install chromium
